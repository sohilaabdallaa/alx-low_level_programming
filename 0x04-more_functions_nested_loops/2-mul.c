#include "main.h"
/**
 * mul - Entry point of program
 * Description: multiplies two numbers
 * @a: integer variable
 * @b: integer variable
 * Return: result of multipilcation
 */
int mul(int a, int b)
{return (a * b);
}
